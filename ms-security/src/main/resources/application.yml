server:
  port: 8081
  
  
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/securitydb?createDatabaseIfNotExist=true&serverTimezone=UTC 
    username: root
    password: root
    
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect 
     